using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Interactable : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        OnTriggerEnter(GetKeyDown)
        
    }
}
